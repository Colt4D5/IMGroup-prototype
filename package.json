{
  "name": "imgroup",
  "private": true,
  "version": "0.0.1",
  "scripts": {
    "dev": "vite build --watch & browser-sync start --proxy 'imgroup-theme.local' --files '[src/scss/**/*.scss, src/js/**/*.js]'",
    "build": "vite build",
    "watch": "vite build --watch",
    "sync": "browser-sync start --proxy 'imgroup-theme.local' --files '[src/scss/**/*.scss, src/js/**/*.js]'"
  },
  "devDependencies": {
    "browser-sync": "^2.27.8",
    "sass": "^1.49.9",
    "vite": "^2.8.0"
  },
  "dependencies": {
    "swiper": "^8.0.7"
  }
}
