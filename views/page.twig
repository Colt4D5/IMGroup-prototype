{% extends "base.twig" %}

{% block header %}
	{# {% include 'headers/header.1.twig' %} #}
	{# {% include 'headers/header.2.twig' %} #}
	{{ parent() }}
	{% include 'components/headerimage.1.twig' %}
{% endblock %}

{% block sidebar %}
	{% include 'sidebar.twig' %}
{% endblock %}

{% block content %}
		<article class="post-type-{{post.post_type}}" id="post-{{post.ID}}">
			<section aria-labelledby="page-title" class="article-content">
				<h1 id="page-title" class="page-title">{{post.title}}</h1>
				<div class="article-body">
					{{post.get_content}}
				</div>
			</section>
		</article>
{% endblock %}